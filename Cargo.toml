[package]
name = "bose-nc"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
anyhow = "1"

[build-dependencies]
cc = "1"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
